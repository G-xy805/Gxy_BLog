---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { Icon } from "astro-icon/components";
import FriendCard from "@/components/mdx/FriendCard.astro";
import Card from "@/components/temple/Card.astro";
import { commentsConfig } from "@config";
import Waline from "@/components/comments/Waline.astro";

const friends = [
  {
    name: "Kael",
    avatar: "/goat.png",
    description: "一个技能树越点越歪的个人博主😜",
    url: "/"
  },
  {
    name: "Firefly",
    avatar: "https://zayck-img.pages.dev/file/1765005605410_BandiView_firefly.webp",
    description: "虚位以待！",
    url: "https://zayck-firefly.pages.dev"
  },
];

---

<BaseLayout title="Friends">
  <!-- Friends Section -->
  <Card class="pt-6 pr-6 pl-6">
      <div class="space-y-6">
        <section>
          <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
            <Icon name="feather:users" class="w-6 h-6 text-primary" />
            <span>友人帐</span>
          </h2>

          
          <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
            {friends.map((friend) => (
              <FriendCard
                name={friend.name}
                avatar={friend.avatar}
                description={friend.description}
                url={friend.url}
              />
            ))}
          </div>
          <div class="divider my-8">
            <Icon name="feather:heart" class="w-10 h-10 text-primary" />
          </div>  
        </section>
      
      </div>
  </Card>


    <Card class="p-6">
      <div class="space-y-6">
        <section>
            <h2 class="text-2xl font-bold mb-4 flex items-center gap-2">
              <Icon name="feather:link" class="w-6 h-6 text-primary" />
              <span>友链申请</span>
            </h2>
        
            <p class="text-base-content/70 mb-4">
              申请友链前<strong class="text-primary">请先在自己网站添加本站友链</strong>，请使用以下信息：
            </p>

            <!-- 可复制的代码块 -->
            <div class="relative group mb-4">
              <pre class="bg-gray-100 dark:bg-gray-800 text-base-content rounded-lg p-4 overflow-x-auto text-sm"><code id="link-code">名称：Zayck' Blog
网址：https://zayck.pages.dev/
头像（方）：https://zayck-img.pages.dev/file/1764984969206_logo.png
头像（圆）：https://zayck-img.pages.dev/file/1765794621905_goat.webp
描述：一个技能树越点越歪的个人博主😜</code></pre>
              <button 
                id="copy-btn" 
                class="absolute top-2 right-2 px-3 py-1 bg-primary text-primary-content rounded-md text-xs opacity-0 group-hover:opacity-100 transition-opacity hover:bg-primary-focus"
                onclick="copyCode()"
              >
                <Icon name="feather:copy" class="w-4 h-4" />
                复制
              </button>
            </div>

            <p class="text-base-content/70 mb-4">
              请在添加友链时使用以上信息，确保信息准确无误。添加完成后，请通过<strong class="text-primary">下方评论或其他联系方式</strong>告知我，<strong class="text-primary">并提供你的网站信息</strong>。我会尽快添加你的友链。
            </p>
            <ul class="list-disc list-inside text-base-content/70 space-y-2">
              <li>互换原则：请先将本站添加到你的友链页面，我会在确认后添加你的友链</li>
              <li>链接维护：如发现友链网站长期无法访问或内容违规，将会移除友链</li>
              <li>网站内容积极向上，无违法违规信息</li>
              <li>不允许含有病毒、木马、恶意代码、弹出广告以及任何危害他人计算机安全的内容</li>
              <li>不能有任何含色情/反动/暴力等不法内容</li>
              <li>网站能在国内正常访问，加载速度合理</li>
              <li>网站有持续的内容更新，不是废弃站点</li>
              <li>网站支持HTTPS访问</li>
              <li>网站以原创内容为主，非纯采集站</li>
              <li>有15篇以上的原创文章</li>
            </ul>
          </section>
        </div>
    </Card>


    <!-- Comments Section -->
    <Card class="pt-6 pr-6 pl-6">
      <div class="space-y-6">
        <section>
          <h2 class="text-xl font-bold mb-4 flex items-center gap-2">
              <Icon name="feather:message-circle" class="w-5 h-5 text-primary" />
              <span>评论</span>
            </h2>
            {commentsConfig?.enable && commentsConfig.type === "waline" && commentsConfig.waline && (
              <Waline {...commentsConfig.waline} marginBottom="1.5rem" />
            )}
        </section>
      </div>
    </Card>

    <!-- 复制代码的JavaScript函数 -->
    <script is:inline>
      // 直接定义全局函数，确保按钮可以调用
      async function copyCode() {
        // 直接定义要复制的内容
        const codeText = `名称：Zayck' Blog
网址：https://zayck.pages.dev
头像（方）：https://zayck-img.pages.dev/file/1764984969206_logo.png
头像（圆）：https://zayck-img.pages.dev/file/1765794621905_goat.webp
描述：一个技能树越点越歪的个人博主😜`;
        
        try {
          // 使用现代 Clipboard API 复制内容
          await navigator.clipboard.writeText(codeText);
          
          // 获取复制按钮
          const copyBtn = document.getElementById('copy-btn');
          // 保存原始内容
          const originalContent = copyBtn.innerHTML;
          
          // 显示复制成功提示
          copyBtn.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-4 h-4"><polyline points="20 6 9 17 4 12"></polyline></svg> 已复制';
          copyBtn.classList.remove('bg-primary');
          copyBtn.classList.add('bg-success');
          
          // 2秒后恢复原状
          setTimeout(() => {
            copyBtn.innerHTML = originalContent;
            copyBtn.classList.remove('bg-success');
            copyBtn.classList.add('bg-primary');
          }, 2000);
        } catch (err) {
          alert('复制失败，请手动选择内容复制');
        }
      }
      
      // 页面加载完成后绑定事件
      document.addEventListener('DOMContentLoaded', function() {
        const copyBtn = document.getElementById('copy-btn');
        if (copyBtn) {
          copyBtn.addEventListener('click', copyCode);
        }
      });
    </script>
</BaseLayout>
