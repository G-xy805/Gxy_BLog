---
import { siteConfig } from "@/config";

// 在组件顶部定义函数，以便在模板中调用
function getWelcomeMessage() {
	const now = new Date();
	const hour = now.getHours();

	if (hour < 6) {
		return "🌙 深夜了，注意休息！";
	}
	if (hour < 9) {
		return "🌅 早上好，开启美好的一天！";
	}
	if (hour < 12) {
		return "☀️ 上午好，今天也要加油！";
	}
	if (hour < 14) {
		return "🍱 中午好，记得吃午饭！";
	}
	if (hour < 18) {
		return "🌤️ 下午好，保持专注！";
	}
	if (hour < 22) {
		return "🌆 晚上好，回顾一天的收获！";
	}
	return "🌙 夜深了，早点休息！";
}
---

<div class="dynamic-welcome onload-animation mb-6 card-base p-6 rounded-2xl relative z-10 overflow-hidden group">
  <!-- 背景装饰 -->
  <div class="absolute inset-0 bg-gradient-to-br from-white to-blue-50 dark:from-gray-800 dark:to-gray-900 opacity-50 group-hover:opacity-100 transition-opacity duration-500"></div>
  
  <div class="relative z-10">
    <h2 class="welcome-title text-2xl font-bold mb-2 flex items-center gap-2">
      <span class="welcome-icon"></span>
      <span class="welcome-text bg-gradient-to-r from-blue-500 to-blue-700 dark:from-blue-400 dark:to-blue-600 bg-clip-text text-transparent"></span>
    </h2>
    <p class="welcome-subtitle text-gray-600 dark:text-gray-300 transition-colors duration-300">
      探索技术边界，记录成长轨迹，分享学习心得。
    </p>
  </div>
</div>

<script>
  // 更新欢迎语
  function getWelcomeMessage() {
    const now = new Date();
    const hour = now.getHours();
    
    if (hour < 6) {
      return { icon: "🌙", text: "深夜了，注意休息！" };
    } else if (hour < 9) {
      return { icon: "🌅", text: "早上好，开启美好的一天！" };
    } else if (hour < 12) {
      return { icon: "☀️", text: "上午好，今天也要加油！" };
    } else if (hour < 14) {
      return { icon: "🍱", text: "中午好，记得吃午饭！" };
    } else if (hour < 18) {
      return { icon: "🌤️", text: "下午好，保持专注！" };
    } else if (hour < 22) {
      return { icon: "🌆", text: "晚上好，回顾一天的收获！" };
    } else {
      return { icon: "🌙", text: "夜深了，早点休息！" };
    }
  }
  
  document.addEventListener('DOMContentLoaded', () => {
    const iconElement = document.querySelector('.welcome-icon');
    const textElement = document.querySelector('.welcome-text');
    
    if (iconElement && textElement) {
      const message = getWelcomeMessage();
      iconElement.textContent = message.icon;
      textElement.textContent = message.text;
    }
  });
</script>